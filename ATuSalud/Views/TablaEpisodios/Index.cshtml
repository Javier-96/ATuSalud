@model IEnumerable<ATuSalud.Models.TablaEpisodios>

@{
    ViewData["Title"] = "Index";
}

<p class="btn btn-info">
    <a asp-action="Create" style="color: white;">Añadir episodio</a>
</p>
<table class="table table-striped">
    <thead class="bg-info text-light">
        <tr>
            <th>
                Paciente Id
            </th>
            <th>
                Fecha inicio
            </th>
            <th>
                Fecha final
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Motivo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Causa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medicación)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PacienteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFinal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Motivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Causa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Medicación)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Editar</a>|
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Id">Detalles</a>|
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
