@model IEnumerable<ATuSalud.Models.TablaMedicamentos>

@{
    ViewData["Title"] = "Index";
}


<p class="btn btn-info">
    <a asp-action="Create" style="color: white;">Añadir medicamento</a>
</p>

<table class="table table-striped text-center">
    <thead class="bg-info text-light">
        <tr>
            <th>
                Nombre
            </th>
            <th>
                Observaciones
            </th>
            <th>
                Efecto secundario
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre_medicamento)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Forma_farmaceutica)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EfectoSec.nombre_efecto)
                </td>
                <td>
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                    <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Id">Detalles</a> |
                    <a class="btn btn-danger"asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
